# Google Drive Integration (Optional)
# Required for Google Drive upload functionality
# Get these from Google Cloud Console: https://console.cloud.google.com/
# 1. Enable Google Drive API and Google Docs API
# 2. Create OAuth 2.0 credentials and API key
# 3. Add authorized JavaScript origins: http://localhost:3000
# Note: These need NEXT_PUBLIC_ prefix for client-side OAuth authentication

NEXT_PUBLIC_GOOGLE_CLIENT_ID=your_google_client_id_here
NEXT_PUBLIC_GOOGLE_API_KEY=your_google_api_key_here

# Gemini AI Configuration
# Required: Get your API key from https://makersuite.google.com/app/apikey
# Note: Server-only (no NEXT_PUBLIC_ prefix) for security - processed via API routes
# The MY_ prefix is necessary because I have a GEMINI_API_KEY in my .zshrc
MY_GEMINI_API_KEY=your_gemini_api_key_here

# API Quota Monitoring (Optional)
# Required for real-time API usage tracking with visual indicators
# Get these from Google Cloud Console project settings
# 1. Enable Service Usage API and Cloud Monitoring API
# 2. Create service account with roles: Service Usage Consumer, Monitoring Viewer, Service Management Service Agent
# 3. Download service account JSON key or set path
# Note: Project Number is different from Project ID (find in project settings)

NEXT_PUBLIC_GOOGLE_PROJECT_NUMBER=your_project_number_here
# Option 1: Paste entire JSON content as string (recommended for deployment)
GOOGLE_SERVICE_ACCOUNT_KEY='{"type":"service_account","project_id":"your-project",...}'
# Option 2: Path to JSON file (for local development)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json